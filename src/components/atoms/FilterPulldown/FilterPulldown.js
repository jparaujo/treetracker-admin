import React from 'react'
import { VisibilityFilters } from '../../actions/ui'
import { toSentence } from 'underscore.string'

const FilterPulldown = fuction(active, children, handleChange) {
  return (<select onChange={handleChange}>ded</select>)
}
​
